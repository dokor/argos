services:
  lighthouse-service:
    build:
      context: /srv/apps/lighthouse-service/src
      dockerfile: Dockerfile
    container_name: lighthouse-service
    restart: unless-stopped
    environment:
      - PORT=3017
      - NODE_ENV=production
      # optionnel : limites / timeouts
      - MAX_CONCURRENCY=2
      - NAV_TIMEOUT_MS=45000
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3017/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    networks:
      - argos_internal

networks:
  argos_internal:
    external: true